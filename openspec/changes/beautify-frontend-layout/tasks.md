## 1. 视觉基础

- [x] 1.1 在 `src/styles.css` 中定义统一的颜色、字体、间距、圆角、边框、阴影和焦点令牌，并验证基础页面与主要表面使用新的层级关系，同时不改变应用数据行为。
- [x] 1.2 让共享按钮、输入框、选择框、文本域、图标控件、标签和表面样式统一使用这些令牌，并验证悬停、键盘焦点、激活、禁用、选中和危险操作状态都能被清晰区分。

## 2. 应用外壳与投递记录工作区

- [x] 2.1 调整 `src/App.vue` 中的应用外壳，使桌面导航、页面标题、主要操作和次要操作具有明确顺序，并验证投递记录页和个人信息页都能正确显示当前导航状态。
- [x] 2.2 重设计投递统计带、搜索与筛选工具栏、空状态和分页区域，并验证零记录和无匹配结果时都能说明原因并提供正确的下一步操作。
- [x] 2.3 优化 `src/components/RecordTable.vue`，明确公司/职位、状态、时间、下一步、链接和行操作的扫描顺序，并验证编辑、状态历史、Markdown、外部链接和删除操作仍然触发原有事件。
- [x] 2.4 在受限桌面窗口中将表格溢出限制在记录工作面内部，并验证页面外壳、顶部区域、筛选控件和分页保持稳定，同时表格内容仍可检查。

## 3. 弹窗与表单表面

- [x] 3.1 将 `src/components/RecordModal.vue` 的字段按清晰意图分组，并保持稳定的底部操作区；验证必填校验、取消、保存和已有草稿值均保持原有行为。
- [x] 3.2 重做 `src/components/HistoryModal.vue`，让节点列表、当前节点详情、新增/删除操作和底部操作区具有明显区分，并验证时间线排序、编辑和链接操作保持不变。
- [x] 3.3 使用统一弹窗语言重做 `src/components/OptionsModal.vue` 和 `src/components/MarkdownModal.vue`，并验证选项编辑、Markdown 编辑/预览、关闭和保存操作仍然可用。
- [x] 3.4 检查招聘信息和状态预览工具提示的对比度、边界定位和溢出处理，并验证较长备注和 Markdown 内容不会逃出桌面窗口。

## 4. 个人信息工作区

- [x] 4.1 重设计 `src/components/PersonalInfoPanel.vue` 的分组标题、数量、添加控件和空状态，使分组层级清晰且不再出现大面积无意义空白，并验证展开/收起和分组编辑仍然有效。
- [x] 4.2 优化有内容时的个人信息行和选中反馈，并验证复制、多选、编辑、拖拽排序和删除操作始终可见且不会遮挡文本。

## 5. 无障碍与回归整理

- [x] 5.1 检查导航、表格、仅图标控件、表单、弹窗和个人信息操作的可访问名称、键盘焦点、对比度和顺序，并验证主要工作流无需只依赖鼠标即可完成。
- [x] 5.2 验证状态、优先级、选中、拖拽、禁用和危险操作除了颜色之外还使用文字或结构线索表达，并通过低色彩或高对比度检查确认界面仍然易懂。

## 6. 验证与交付

- [x] 6.1 运行现有 Vite 生产构建，并确认视觉改动没有引入警告或错误。
- [x] 6.2 使用零条、单条和多条投递记录，以及空的和有内容的个人信息分组进行验收，并在宽屏和受限桌面窗口尺寸下检查截图是否符合 `job-tracker-interface` 要求。
- [x] 6.3 验证视觉刷新后导入导出、本地持久化、状态时间线、Markdown 内容和现有操作事件仍然正常，并记录交付前的剩余视觉限制。
