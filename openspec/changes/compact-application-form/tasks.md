## 1. 紧凑表单结构

- [x] 1.1 重组 `src/components/RecordModal.vue` 的新增投递字段为桌面端紧凑网格，并验证公司、职位、地点、优先级、来源、其他来源说明、招聘信息、状态、下一步和备注仍绑定到原有草稿字段。
- [x] 1.2 保留新增投递的必填属性、历史建议、来源为“其他”时的条件字段、默认状态以及 `close`/`save` 事件，并在浏览器中验证有效保存、缺少必填字段、取消和关闭均维持既有行为。
- [x] 1.3 调整表单标签与 DOM 顺序以匹配从左至右、从上至下的录入流程，并使用键盘 Tab/Shift+Tab 验证焦点可见、历史建议与关闭/取消/保存控件均可访问。

## 2. 桌面端布局与样式

- [x] 2.1 在 `src/styles.css` 中为新增投递弹窗加入专用的桌面端尺寸、三段结构和多列网格规则，并在 `1024×768` 与 `1280×800` 视口中验证表单、关闭、取消和保存无需页面或弹窗整体纵向滚动即可同时访问。
- [x] 2.2 为招聘信息和备注文本域设置稳定可视高度与内部滚动，禁止其通过垂直拉伸或内容增长挤出操作区，并用长文本验证只有对应文本域出现纵向滚动。
- [x] 2.3 检查通用弹窗样式和既有媒体查询不会覆盖目标桌面网格或恢复整体滚动；不新增移动端断点或移动端适配规则，并回归编辑投递模式的字段和操作可见性。

## 3. 回归与交付验证

- [x] 3.1 在 `1024×768` 和 `1280×800` 桌面窗口中分别验证普通来源与“其他”来源的新增投递流程，确认所有字段、错误反馈和主要操作不重叠、不裁切且无需整体滚动。
- [x] 3.2 新增一条含可选字段和长文本的记录，刷新后确认记录内容保持；同时验证取消或必填校验失败不会写入不完整数据。
- [x] 3.3 运行 `npm.cmd run build`、`git diff --check` 和 `openspec validate "compact-application-form" --type change --strict --json`，确认构建、差异检查和变更规格校验通过。
