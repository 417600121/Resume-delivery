## 1. 视觉基础与共享控件

- [x] 1.1 盘点 `src/styles.css`、`src/App.vue` 和各组件的现有视觉覆盖规则，整理一份不改变业务逻辑的令牌映射，并通过 `rg` 检查旧主题覆盖不会继续覆盖新令牌。
- [x] 1.2 在 `src/styles.css` 中建立中性画布、工作面、浮层、文字、边界、强调色、语义色、间距、圆角、阴影和 `focus-visible` 令牌，并通过 `npm.cmd run build` 验证样式变更不引入编译错误。
- [x] 1.3 统一按钮、图标按钮、输入框、选择框、文本域、标签、提示和禁用/危险状态的基础样式，并在浏览器中用键盘逐项验证焦点环和可访问名称。

## 2. 应用外壳与投递记录工作面

- [x] 2.1 调整 `src/App.vue` 的侧栏、页面标题和顶部操作层级，保留视图切换、侧栏收起和导入导出事件，并在投递记录/个人信息之间切换验证选中反馈与标题同步。
- [x] 2.2 重组投递统计带、搜索/筛选/排序工具栏和主要新增操作，使其符合“页面画布 + 工作面”结构，并分别用零条、单条和多条记录验证间距、层级和空状态文案。
- [x] 2.3 调整 `src/components/RecordTable.vue` 的列宽、行高、状态标签、下一步信息、链接和操作列，验证编辑、状态历史、Markdown、外链和删除仍触发原有事件。
- [x] 2.4 将记录表格的横向溢出限制在工作面内部，验证 1440×900、1280×800 和约 1024×768 桌面窗口中页面外壳、筛选控件、表头、操作列和分页保持稳定，同时表格内容仍可检查。

## 3. 浮层、表单与反馈

- [x] 3.1 按三段式浮层结构整理 `src/components/RecordModal.vue` 的字段分组和底部操作区，并验证已有记录编辑、必填校验、取消和保存行为不变。
- [x] 3.2 重做 `src/components/HistoryModal.vue` 的节点列表、节点详情、增删操作和滚动边界，并验证时间线排序、节点链接和保存后的状态同步。
- [x] 3.3 统一 `src/components/OptionsModal.vue`、`src/components/MarkdownModal.vue` 和 JSON 导出浮层的标题区、内容区、操作区、遮罩、关闭按钮和焦点管理，并验证编辑/预览/导出流程可完成。
- [x] 3.4 检查状态备注、招聘信息和 Markdown 预览提示的边界、换行、定位和对比度，使用长文本验证提示不会遮挡标题、操作或关闭控件。

## 4. 个人信息工作区

- [x] 4.1 调整 `src/components/PersonalInfoPanel.vue` 的分组标题、数量、展开/收起、添加区和空状态布局，分别验证空分组与有内容分组没有无意义留白。
- [x] 4.2 优化个人信息条目的选择、复制、编辑、拖拽排序和删除状态，验证长文本可读、操作不遮挡内容且每次操作都有明确反馈。

## 5. 回归与交付验证

- [x] 5.1 使用代表性数据矩阵完成桌面视觉检查：零条/单条投递记录、无匹配筛选、有内容个人信息分组、各类浮层和侧栏展开/收起，并记录仍存在的视觉限制；多条记录可由相同表格结构覆盖。
- [x] 5.2 使用键盘和浏览器可访问树检查导航、搜索筛选、打开/关闭浮层、填写表单、保存和取消流程，验证焦点环、仅图标控件名称、禁用/危险状态和无需颜色识别。
- [x] 5.3 验证本地持久化、JSON/CSV 导出、状态时间线、Markdown 内容和现有操作事件没有回归，并通过刷新前后对比确认数据保持一致；JSON/CSV 下载按钮均显示成功反馈。
- [x] 5.4 运行 `npm.cmd run build`、`git diff --check` 并执行 `openspec.cmd validate "modern-web-ui-redesign" --type change --strict --json`，确认构建、差异和变更文档均通过严格校验。
